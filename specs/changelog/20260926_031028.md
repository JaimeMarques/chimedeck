## Update

Retain PR #10's mixed native PATCH / Trello DELETE race in the current PostgreSQL board-member regression suite. Reuse its deterministic advisory/table-lock barrier and assert exactly one 200, one 409, and one remaining ADMIN. This verifies cross-adapter coordination, not merely each adapter racing itself.

## New

One mixed scenario in `tests/db/concurrentBoardMemberRoleMutation.ts`, already executed by `bun run test:db:board-members` and the board-member CI job. No production behavior changes; keep the merged eligible-admin and stale-GUEST policy unchanged.

Verification: all 13 role-mutation scenarios pass, test-file lint and test-project typecheck pass. Removing workspace and board advisory locks only in an isolated copy makes the mixed case fail with `[200, 200]` instead of `[200, 409]`.

After a fresh frozen-lockfile install, full suite remains 950 pass / 124 fail, and full typecheck remains at 172 errors, matching merged `main` (`3de62f08`), including failing names and normalized diagnostics; those full gates are not green.

## Technical Debt

Inherited full-suite/typecheck failures remain outside this test-only change.

## What Should Be Done Next

Use this test-only replacement when closing the superseded PR #10. Do not restore its stale-GUEST ADMIN preservation expectation: current policy intentionally removes that authority instead of reactivating it.
