name: chimedeck-dev

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "${APP_PORT:-3000}:3000"
    env_file: .env
    volumes:
      - .:/app
      - /app/node_modules         # preserve container node_modules
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    build:
      context: .
      dockerfile: Dockerfile.postgres
    environment:
      POSTGRES_USER: chimedeck
      POSTGRES_PASSWORD: chimedeck
      POSTGRES_DB: chimedeck_dev
    command: >
      postgres -c shared_preload_libraries=pg_cron
               -c cron.database_name=chimedeck_dev
               -c max_connections=200
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chimedeck"]
      interval: 5s
      timeout: 5s
      retries: 10

  localstack:
    # Pin to a community (non-pro) tag: `latest` now requires a licence token
    # and exits 55 without one. 3.8.1 is the last freely usable line and is
    # sufficient for S3. See docs/local-development.md.
    image: localstack/localstack:3.8.1
    ports:
      - "4566:4566"
    environment:
      SERVICES: s3
      DEFAULT_REGION: us-east-1
      S3_BUCKET: ${S3_BUCKET:-chimedeck}
    volumes:
      - localstack_data:/var/lib/localstack
      - ./localstack-init/ready.d:/etc/localstack/init/ready.d:ro

  redis:
    image: redis:7-alpine
    profiles: [redis]         # optional — only started with --profile redis
    ports:
      - "6379:6379"

volumes:
  postgres_data:
  localstack_data:
